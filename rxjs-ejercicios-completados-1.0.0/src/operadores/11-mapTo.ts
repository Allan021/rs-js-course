// import { asapScheduler, interval, Observer } from "rxjs";
// import { mapTo } from "rxjs/operators";
// import { observer } from "./12-filter";

// const counter$ = interval(2000).pipe(mapTo(12));

// const sub = counter$.subscribe(observer);

// asapScheduler.schedule(() => sub.unsubscribe(), 4000);
